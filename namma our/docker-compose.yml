version: '3.8'

services:
  namma_oour_web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - FLUTTER_WEB_PORT=8080
    command: flutter run -d web-server --web-port 8080 --web-hostname 0.0.0.0
